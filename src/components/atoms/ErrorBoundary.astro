---
/**
 * ErrorBoundary.astro — Fail-safe wrapper
 * Catches rendering issues and shows a dark skeleton fallback.
 */
interface Props {
    fallbackTitle?: string;
    class?: string;
}

const { fallbackTitle = '載入失敗', class: className = '' } = Astro.props;
---

<div
    class:list={[
        'glass-card p-6 flex flex-col items-center justify-center gap-3 min-h-[120px]',
        className,
    ]}
>
    <div class="text-bearish text-2xl">⚠️</div>
    <p class="text-sm text-text-secondary font-medium">{fallbackTitle}</p>
    <button class="btn-ghost text-xs" onclick="window.location.reload()"> 🔄 重新載入 </button>
</div>

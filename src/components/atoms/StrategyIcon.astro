---
/**
 * StrategyIcon.astro - Atomic representation of a strategy icon
 */
interface Props {
    icon: string;
    class?: string;
}

const { icon, class: className = '' } = Astro.props;
---

<span class:list={['text-xl leading-none', className]}>
    {icon}
</span>

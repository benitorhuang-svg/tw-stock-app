---
/**
 * Skeleton.astro — Shimmer loading placeholder
 * Zero CLS: always specify explicit height via class or style.
 */
interface Props {
    class?: string;
    /** h-4, h-6, h-8 etc. */
    height?: string;
    /** rounded, rounded-lg, rounded-full */
    rounded?: string;
}

const { class: className = '', height = 'h-4', rounded = 'rounded-md' } = Astro.props;
---

<div class:list={['skeleton', height, rounded, className]} aria-hidden="true"></div>

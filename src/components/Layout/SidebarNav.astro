---
/**
 * 專業金融風格側邊導航欄 - 自動展開優化版
 */

const navItems = [
    // 移除首頁，由 Logo 承擔功能
    { label: '個股分析', icon: '📊', href: '/stocks' },
    { label: '智能篩選', icon: '🔍', href: '/filter' },
    { label: '策略中心', icon: '🎯', href: '/strategies' },
    { label: '股利查詢', icon: '💰', href: '/dividends' },
    { label: '自選股', icon: '⭐', href: '/watchlist' },
    { label: '投資組合', icon: '💼', href: '/portfolio' },
];

const bottomItems = [{ label: '設定', icon: '⚙️', href: '/settings' }];
---

<nav class="sidebar-nav" id="sidebar-nav">
    <!-- Logo Section -->
    <div class="nav-header">
        <a href="/" class="logo">
            <span class="logo-icon">📈</span>
            <span class="logo-text">TW Stock</span>
        </a>
    </div>

    <!-- Main Navigation -->
    <div class="nav-items">
        {
            navItems.map(item => (
                <a href={item.href} class="nav-item">
                    <span class="item-icon">{item.icon}</span>
                    <span class="item-label">{item.label}</span>
                </a>
            ))
        }
    </div>

    <!-- Bottom Settings -->
    <div class="nav-bottom">
        {
            bottomItems.map(item => (
                <a href={item.href} class="nav-item">
                    <span class="item-icon">{item.icon}</span>
                    <span class="item-label">{item.label}</span>
                </a>
            ))
        }
    </div>
</nav>

<style>
    .sidebar-nav {
        width: 70px; /* 預設收合狀態 */
        height: 100vh;
        background: var(--c-bg-glass);
        backdrop-filter: var(--glass-blur);
        border-right: 1px solid var(--c-border);
        display: flex;
        flex-direction: column;
        padding: 16px 12px;
        position: fixed;
        left: 0;
        top: 0;
        z-index: 100;
        transition:
            width 0.35s cubic-bezier(0.16, 1, 0.3, 1),
            box-shadow 0.3s;
        overflow: hidden;
    }

    /* 滑鼠靠近或強制展開時 */
    .sidebar-nav:hover,
    .sidebar-nav.expanded {
        width: 240px;
        box-shadow: 10px 0 30px rgba(0, 0, 0, 0.3);
    }

    /* Logo Text & Item Labels 隱藏與顯示 */
    .logo-text,
    .item-label {
        opacity: 0;
        white-space: nowrap;
        pointer-events: none;
        transition: opacity 0.2s;
    }

    .sidebar-nav:hover .logo-text,
    .sidebar-nav.expanded .logo-text,
    .sidebar-nav:hover .item-label,
    .sidebar-nav.expanded .item-label {
        opacity: 1;
        pointer-events: auto;
        transition: opacity 0.3s 0.1s; /* 延遲顯示文字 */
    }

    /* Header */
    .nav-header {
        display: flex;
        align-items: center;
        padding-bottom: 16px;
        border-bottom: 1px solid var(--c-border);
        margin-bottom: 16px;
        height: 40px;
        overflow: hidden;
    }

    .logo {
        display: flex;
        align-items: center;
        gap: 12px;
        color: var(--c-text-primary);
        font-weight: 800;
        text-decoration: none;
        font-size: 16px;
        width: 100%;
    }

    .logo-icon {
        font-size: 20px;
        min-width: 46px; /* 居中圖示 */
        display: flex;
        justify-content: center;
    }

    /* Nav Items */
    .nav-items {
        display: flex;
        flex-direction: column;
        gap: 4px;
        flex: 1;
    }

    .nav-item {
        display: flex;
        align-items: center;
        gap: 12px;
        padding: 12px;
        border-radius: 8px;
        color: var(--c-text-secondary);
        text-decoration: none;
        font-size: 14px;
        font-weight: 500;
        transition: all 0.2s var(--ease-out);
        height: 48px;
    }

    .nav-item:hover {
        background: rgba(var(--c-accent-rgb), 0.1);
        color: var(--c-text-primary);
    }

    .nav-item.active {
        background: rgba(var(--c-accent-rgb), 0.15);
        color: var(--c-accent);
        font-weight: 700;
    }

    .item-icon {
        font-size: 18px;
        min-width: 46px; /* 與標題圖示寬度一致，確保對齊 */
        text-align: center;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .nav-bottom {
        border-top: 1px solid var(--c-border);
        padding-top: 16px;
    }

    /* 緊湊模式優化 */
    .sidebar-nav:not(:hover):not(.expanded) .nav-item {
        padding: 12px 0;
    }
</style>

<script>
    const sidebarNav = document.getElementById('sidebar-nav');

    // 初始化：首頁預設展開（但滑鼠移開後仍會收合）
    const currentPath = window.location.pathname;
    if (currentPath === '/') {
        sidebarNav?.classList.add('expanded');

        // 滑鼠第一次進入後取消 "強制展開" 狀態，切換為純 hover 邏輯
        const handleFirstEntry = () => {
            sidebarNav?.classList.remove('expanded');
            sidebarNav?.removeEventListener('mouseenter', handleFirstEntry);
        };
        sidebarNav?.addEventListener('mouseenter', handleFirstEntry);
    }

    // 標記 active 項目
    document.querySelectorAll('.nav-item').forEach(item => {
        const href = item.getAttribute('href') || '';
        if (href === currentPath || (href !== '/' && currentPath.startsWith(href))) {
            item.classList.add('active');
        }
    });
</script>

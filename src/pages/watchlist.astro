---
import Layout from '../layouts/Layout.astro';
import { getStocksWithPrices } from '../utils/stockDataService';
import StockCard from '../components/organisms/StockCard.astro';

// Load real stock data
const allStocks = await getStocksWithPrices();

// Format for StockCard
const stockList = allStocks.map(s => ({
    symbol: s.symbol,
    name: s.name,
    price: s.price,
    change: s.change,
    changePercent: s.changePercent,
    volume: s.volume,
    pe: s.pe,
    pb: s.pb,
    yield: s.yield,
    roe: s.roe,
    eps: s.eps,
}));
---

<Layout title="自選股">
    <div class="page-header">
        <h1>⭐ 自選股</h1>
        <button id="clear-all-btn" class="clear-btn">清空全部</button>
    </div>
    <p class="page-desc">您收藏的股票會顯示在這裡，資料儲存在瀏覽器中。</p>

    <!-- 空狀態 -->
    <div id="empty-state" class="empty-state">
        <div class="empty-icon">📋</div>
        <h3>尚無自選股</h3>
        <p>前往<a href="/stocks">個股分析</a>頁面，點擊 ☆ 按鈕加入自選股。</p>
    </div>

    <!-- 自選股列表 -->
    <div id="watchlist-grid" class="stock-grid hidden">
        {
            stockList.map(stock => (
                <div class="watchlist-item" data-symbol={stock.symbol}>
                    <StockCard stock={stock} variant="detailed" />
                </div>
            ))
        }
    </div>

    <!-- 快速操作 -->
    <div id="quick-actions" class="quick-actions hidden">
        <h2>🎯 快速篩選</h2>
        <div class="filter-buttons">
            <button class="filter-btn" data-filter="high-yield">高殖利率 (&gt;5%)</button>
            <button class="filter-btn" data-filter="low-pe">低本益比 (&lt;15)</button>
            <button class="filter-btn" data-filter="high-roe">高 ROE (&gt;15%)</button>
        </div>
    </div>
</Layout>

<style>
    .page-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 8px;
    }

    .clear-btn {
        padding: 8px 16px;
        background: rgba(255, 71, 87, 0.1);
        border: 1px solid rgba(255, 71, 87, 0.3);
        border-radius: 8px;
        color: #ff4757;
        cursor: pointer;
        font-size: 0.85rem;
        transition: all 0.15s;
    }

    .clear-btn:hover {
        background: rgba(255, 71, 87, 0.2);
    }

    .page-desc {
        color: var(--text-secondary);
        margin-bottom: 32px;
    }

    .empty-state {
        text-align: center;
        padding: 60px 24px;
        background: var(--bg-card);
        border: 1px dashed var(--border);
        border-radius: 16px;
    }

    .empty-icon {
        font-size: 4rem;
        margin-bottom: 16px;
    }

    .empty-state h3 {
        color: var(--text-primary);
        margin-bottom: 8px;
    }

    .empty-state p {
        color: var(--text-secondary);
    }

    .empty-state a {
        color: var(--accent);
    }

    .stock-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
        gap: 20px;
        margin-bottom: 48px;
    }

    .watchlist-item.hidden {
        display: none;
    }

    .quick-actions {
        background: var(--bg-card);
        border: 1px solid var(--border);
        border-radius: 16px;
        padding: 24px;
    }

    .quick-actions h2 {
        font-size: 1.1rem;
        margin-bottom: 16px;
    }

    .filter-buttons {
        display: flex;
        flex-wrap: wrap;
        gap: 12px;
    }

    .filter-btn {
        padding: 10px 20px;
        background: rgba(0, 212, 170, 0.1);
        border: 1px solid rgba(0, 212, 170, 0.3);
        border-radius: 8px;
        color: var(--accent);
        cursor: pointer;
        font-size: 0.9rem;
        transition: all 0.15s;
    }

    .filter-btn:hover,
    .filter-btn.active {
        background: var(--accent);
        color: #000;
    }

    .hidden {
        display: none !important;
    }
</style>

<script>
    const watchlistGrid = document.getElementById('watchlist-grid')!;
    const emptyState = document.getElementById('empty-state')!;
    const quickActions = document.getElementById('quick-actions')!;
    const clearAllBtn = document.getElementById('clear-all-btn')!;

    // 初始渲染
    function renderWatchlist() {
        const watchlist = JSON.parse(localStorage.getItem('tw-watchlist') || '[]') as string[];

        // 隱藏所有項目
        document.querySelectorAll('.watchlist-item').forEach(item => {
            const symbol = item.getAttribute('data-symbol');
            if (watchlist.includes(symbol!)) {
                item.classList.remove('hidden');
            } else {
                item.classList.add('hidden');
            }
        });

        // 顯示/隱藏空狀態
        if (watchlist.length === 0) {
            emptyState.classList.remove('hidden');
            watchlistGrid.classList.add('hidden');
            quickActions.classList.add('hidden');
        } else {
            emptyState.classList.add('hidden');
            watchlistGrid.classList.remove('hidden');
            quickActions.classList.remove('hidden');
        }
    }

    // 初始化
    renderWatchlist();

    // 監聽自選股更新事件
    window.addEventListener('watchlist-updated', () => {
        renderWatchlist();
    });

    // 清空全部
    clearAllBtn.addEventListener('click', () => {
        if (confirm('確定要清空所有自選股嗎？')) {
            localStorage.setItem('tw-watchlist', '[]');
            renderWatchlist();

            // 更新所有星星按鈕
            document.querySelectorAll('.watchlist-btn').forEach(btn => {
                btn.classList.remove('active');
                (btn.querySelector('.star') as HTMLElement).textContent = '☆';
            });
        }
    });

    // 快速篩選
    document.querySelectorAll('.filter-btn').forEach(btn => {
        btn.addEventListener('click', () => {
            const filter = btn.getAttribute('data-filter');

            // 切換 active 狀態
            if (btn.classList.contains('active')) {
                btn.classList.remove('active');
                renderWatchlist(); // 恢復原狀
                return;
            }

            document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
            btn.classList.add('active');

            const watchlist = JSON.parse(localStorage.getItem('tw-watchlist') || '[]') as string[];

            document.querySelectorAll('.watchlist-item').forEach(item => {
                const symbol = item.getAttribute('data-symbol');
                if (!watchlist.includes(symbol!)) {
                    item.classList.add('hidden');
                    return;
                }

                // 這裡需要從 DOM 或資料取得股票資訊來篩選
                // 簡化版本：顯示所有
                item.classList.remove('hidden');
            });
        });
    });
</script>

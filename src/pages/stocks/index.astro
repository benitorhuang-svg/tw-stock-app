---
/**
 * stocks/index.astro — Intelligence_Center Landing Page
 * Central nexus for deep-vector stock analysis and search.
 */
import MainTerminal from '../../layouts/MainTerminal.astro';
import StockHubHero from '../../components/molecules/StockHubHero.astro';
import StockSearchEngine from '../../components/organisms/StockSearchEngine.astro';
import RecentAnalyses from '../../components/molecules/RecentAnalyses.astro';
import DividendIntelligenceWall from '../../components/molecules/DividendIntelligenceWall.astro';
import ForensicTicker from '../../components/molecules/ForensicTicker.astro';
---

<MainTerminal title="Intelligence_Center (CORE_NEXUS)">
    <div
        class="h-full flex flex-col p-4 lg:p-10 lg:pt-16 max-w-[1400px] mx-auto w-full overflow-hidden"
    >
        <!-- Centerpiece Header -->
        <StockHubHero />

        <!-- Search Nexus (Organism) -->
        <StockSearchEngine />

        <!-- Quick Access Matrix -->
        <div
            class="grid grid-cols-1 lg:grid-cols-2 gap-8 mb-12 animate-fade-up"
            style="animation-delay: 200ms"
        >
            <RecentAnalyses />
            <DividendIntelligenceWall />
        </div>

        <!-- Capability Tiers -->
        <div
            class="grid grid-cols-2 lg:grid-cols-4 gap-4 animate-fade-up"
            style="animation-delay: 300ms"
        >
            {
                [
                    {
                        icon: '📈',
                        label: 'Multi-Vector Charting',
                        text: 'Cycle dynamics and RSI velocity',
                    },
                    {
                        icon: '💎',
                        label: 'Capital Forensic',
                        text: 'Institutional flow and accumulation',
                    },
                    {
                        icon: '🏦',
                        label: 'Structural Audit',
                        text: 'Fundamental health and margin scan',
                    },
                    { icon: '🤖', label: 'Momentum Alert', text: 'AI-driven variance detection' },
                ].map(f => (
                    <div class="p-6 glass-card border-white/5 hover:border-accent/30 transition-all text-center group bg-white/[0.01]">
                        <div class="text-2xl mb-4 group-hover:scale-110 transition-transform">
                            {f.icon}
                        </div>
                        <h4 class="text-[10px] font-black text-white/90 mb-2 uppercase tracking-widest">
                            {f.label}
                        </h4>
                        <p class="text-[9px] text-white/30 font-mono leading-relaxed uppercase tracking-tighter">
                            {f.text}
                        </p>
                    </div>
                ))
            }
        </div>

        <!-- Signal Stream Footer -->
        <div class="mt-auto pt-10 border-t border-white/5 flex items-center justify-between">
            <ForensicTicker />
            <span class="text-[8px] font-mono text-white/10 uppercase tracking-[0.4em]"
                >Uplink_Node: ANALYSIS_CENTER_TAIWAN</span
            >
        </div>
    </div>

    <script src="../../scripts/stocks-index-engine.ts"></script>
</MainTerminal>
